package androidx.cardview.widget;

import android.content.Context;
import android.content.res.ColorStateList;

/* JADX INFO: loaded from: /content/repo2/apk-analysis/Alight motion /classes2.dex */
interface CardViewImpl {
    float J2(CardViewDelegate cardViewDelegate);

    void KN(CardViewDelegate cardViewDelegate, Context context, ColorStateList colorStateList, float f3, float f4, float f5);

    ColorStateList O(CardViewDelegate cardViewDelegate);

    void Uo(CardViewDelegate cardViewDelegate);

    void az(CardViewDelegate cardViewDelegate, ColorStateList colorStateList);

    void gh();

    void mUb(CardViewDelegate cardViewDelegate);

    void n(CardViewDelegate cardViewDelegate, float f3);

    float nr(CardViewDelegate cardViewDelegate);

    float qie(CardViewDelegate cardViewDelegate);

    float rl(CardViewDelegate cardViewDelegate);

    void t(CardViewDelegate cardViewDelegate, float f3);

    void ty(CardViewDelegate cardViewDelegate, float f3);

    float xMQ(CardViewDelegate cardViewDelegate);
}
